# AI-chart-Agent
AI chart Agent

Small chat-based AI agent (CLI).
- Uses an OpenAI-compatible API key from environment variable OPENAI_API_KEY.
- Simple local session memory saved as JSON files in ./sessions/.
- Conversation loop with system prompt, optional persona, and history trimming.
- Easily extendable to web / GUI / tools.
